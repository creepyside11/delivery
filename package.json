{
  "name": "snowpost-delivery",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/game.js && node --check src/world.js && node --check src/rules.js",
    "build": "node scripts/build.mjs"
  }
}
